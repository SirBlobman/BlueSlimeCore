<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    
    <parent>
        <groupId>com.SirBlobman.api.hook.factions</groupId>
        <artifactId>handler-factions-parent</artifactId>
        <version>1.13-SNAPSHOT</version>
        <relativePath>../</relativePath>
    </parent>

    <artifactId>handler-Factions</artifactId>
    <name>Hook Handler - Factions</name>
    <description>The Factions hook handler.</description>
    
    <repositories>
        <!-- SpigotMC Repository -->
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        
        <!-- CodeMC Public Repository -->
        <repository>
            <id>codemc-public</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>
    </repositories>
    
    <dependencies>
        <!-- Spigot API -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- FactionsUUID Hook -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-FactionsUUID</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>

        <!-- FactionsX Hook -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-FactionsX</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>

        <!-- LegacyFactions Hook -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-LegacyFactions</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>

        <!-- MassiveCore Hook -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-MassiveCore</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>
</project>