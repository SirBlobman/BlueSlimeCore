<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    
    <parent>
        <groupId>com.SirBlobman.api.hook.factions</groupId>
        <artifactId>handler-factions-parent</artifactId>
        <version>1.13-SNAPSHOT</version>
        <relativePath>../</relativePath>
    </parent>

    <artifactId>handler-factions-MassiveCore</artifactId>
    <name>Hook Handler - MassiveCore Factions</name>
    <description>The MassiveCore Factions hook handler.</description>
    
    <repositories>
        <!-- SpigotMC Repository -->
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        
        <!-- CodeMC Public Repository -->
        <repository>
            <id>codemc-public</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>

        <!-- MassiveCore Repository -->
        <repository>
            <id>massivecraft-team</id>
            <url>https://maven.massivecraft.team/repository/maven-releases/</url>
        </repository>
    </repositories>
    
    <dependencies>
        <!-- Spigot API -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Hook Handler -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-hook</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>
        
        <!-- Factions Dummy API -->
        <dependency>
            <groupId>com.SirBlobman.api.dummy</groupId>
            <artifactId>Factions</artifactId>
            <version>2.14.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>