<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    
    <parent>
        <groupId>com.SirBlobman.api.hook.factions</groupId>
        <artifactId>handler-factions-parent</artifactId>
        <version>1.13-SNAPSHOT</version>
        <relativePath>../</relativePath>
    </parent>
    
    <groupId>com.SirBlobman.api.hook.factions</groupId>
    <artifactId>handler-factions-FactionsUUID</artifactId>
    
    <name>Hook Handler - Factions UUID</name>
    <description>The FactionsUUID hook handler.</description>
    
    <repositories>
        <!-- SpigotMC Repository -->
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        
        <!-- CodeMC Public Repository -->
        <repository>
            <id>codemc-public</id>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>

        <!-- EnderZone Repository -->
        <repository>
            <id>ender-zone</id>
            <url>https://ci.ender.zone/plugin/repository/everything/</url>
        </repository>
    </repositories>
    
    <dependencies>
        <!-- Spigot API -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Hook Handler -->
        <dependency>
            <groupId>com.SirBlobman.api.hook.factions</groupId>
            <artifactId>handler-factions-hook</artifactId>
            <version>1.13-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>

        <!-- Factions UUID -->
        <dependency>
            <groupId>com.massivecraft</groupId>
            <artifactId>Factions</artifactId>
            <version>1.6.9.5-U0.5.10</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>