<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.SirBlobman.api.nms</groupId>
        <artifactId>nms-parent</artifactId>
        <version>2.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>nms-1_15_R1</artifactId>
    <name>SirBlobmanAPI NMS 1.15.2</name>

    <properties>
        <spigot.version>1.15.2-R0.1-SNAPSHOT</spigot.version>
    </properties>

    <repositories>
        <!-- CodeMC NMS Repository -->
        <repository>
            <id>codemc-nms</id>
            <url>https://repo.codemc.io/repository/nms/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Spigot NMS -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- SirBlobmanAPI Abstract NMS -->
        <dependency>
            <groupId>com.SirBlobman.api.nms</groupId>
            <artifactId>nms-abstract</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- SirBlobmanAPI Item -->
        <dependency>
            <groupId>com.SirBlobman.api</groupId>
            <artifactId>item</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>
</project>