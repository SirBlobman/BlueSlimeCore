<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    
    <parent>
        <groupId>com.SirBlobman.api</groupId>
        <artifactId>SirBlobmanAPI-parent</artifactId>
        <version>1.12-SNAPSHOT</version>
        <relativePath>../../</relativePath>
    </parent>
    
    <artifactId>NMS-fallback</artifactId>
    <name>SirBlobmanAPI NMS Fallback Version</name>
    <description>A fallback NMS version used when a real NMS version could not be detected.</description>
    
    <properties>
        <spigot.version>1.8-R0.1-SNAPSHOT</spigot.version>
    </properties>
    
    <dependencies>
        <!-- Spigot API -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- SirBlobmanAPI NMS Handler -->
        <dependency>
            <groupId>com.SirBlobman.api</groupId>
            <artifactId>NMS-handler</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>